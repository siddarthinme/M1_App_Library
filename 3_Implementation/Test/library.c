#include "library.h"
int div(int num1, int num2)
{
    if(num2 == 0)
        return 0;
    return num1/num2;
}
int login(int username, int password)
{
    if(username==1 && password==1))
        return 1;
    else
        return 0;
}